{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "ConnectionStrings": {
        // For local development, you might use different credentials or database names
        "DefaultConnection": "Host=localhost;Port=5432;Database=EmailEZDevDb;Username=postgres;Password=postgres",
        "HangfireConnection": "Host=localhost;Port=5432;Database=EmailEZDev-HangfireDb;Username=postgres;Password=postgres"
    }
    // SmtpSettings would typically be configured globally for the service, but if you have dev-specific SMTP
    // e.g., using Mailtrap, you could override it here.
}